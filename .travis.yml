language: python

matrix:
  include:
    - python: 3.5
  fast_finish: true

services: mongodb

install:
  - pip install --upgrade -r dev_requirements.txt
  - python setup.py install

script:
  - py.test test
